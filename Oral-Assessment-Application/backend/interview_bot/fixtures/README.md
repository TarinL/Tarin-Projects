# Fixtures

Example and sample data used for testing and as references — not used by the bot at runtime.

- `sample_submission.txt` — example student submission (assignment-review mode).
- `example_kb.txt` — example knowledge-base source for `kb_generator.py`.
- `example_assessment.json` — example generated assessment/rubric.
- `db_test_call.json` — sample payload for testing the Zoom integration API.
- `interview_config.json.bak` — a saved copy of an `interview_config.json`.
